meta {
  name: Create Note
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/notes
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Test Title",
    "content": "This is a test note"
  }
  
  
}

assert {
  res.status: eq 201
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
